pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    // Loader untuk projek Flutter (ini ada dalam Flutter SDK anda)
    id "dev.flutter.flutter-plugin-loader" version "1.0.0"
}

include(":app")

// Ini penting → suruh Gradle cari plugin loader dari Flutter SDK tempatan
includeBuild("../..")
